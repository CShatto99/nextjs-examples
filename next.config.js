/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  // swcMinify: true,
  // experimental: {
  //   // ssr and displayName are configured by default
  //   // styledComponents: true // ports babel-plugin-styled-components to the Next.js compiler
  // },
};
